# Черновики и архив

Материалы, которые не публикуются: перенесены со старого сайта «на всякий случай» или ещё не готовы. Папка есть только в рабочей ветке `Katya`: при слиянии в `master` её удаляет `strip-internal.py`, в `index.json` она не попадает (не входит в корни контента). Решение Кати, 14.09.2026.

- «Категории обращения» — статья с сайта (27.06.2022), раздел «Обращения» старой базы; на неё ссылалась «Работа с задачами».
